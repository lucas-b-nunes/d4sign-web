# URL pública da API (fallback SSR). Em dev local, pode ser localhost ou ngrok.
NEXT_PUBLIC_API_URL="http://localhost:3003"

# URL interna para SSR e proxy /api/backend (Next.js → d4sign-api).
# Dev: localhost:3003. Produção (Vercel): URL da API em produção.
API_INTERNAL_URL="http://localhost:3003"
